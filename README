Utf8Checker checks if a file or stream is encoded in UTF8.

Currently it scans the whole provided file/stream by loading it directly in memory and therefore it isn't recommended to be 
used without modifications in scenarios where performance is important.